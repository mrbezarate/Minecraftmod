# Minecraft Bot - Полнофункциональный бот на Node.js

Полнофункциональный Minecraft бот, написанный на Node.js с использованием библиотеки Mineflayer.

## 🚀 Возможности

- ✅ Автоматическая добыча ресурсов (дерево, камень)
- ✅ Автоматический крафт предметов
- ✅ Умное передвижение с помощью pathfinder
- ✅ Команды в чате для управления ботом
- ✅ Автоматическое переподключение при отключении
- ✅ Мониторинг здоровья и голода
- ✅ Следование за игроками
- ✅ Обработка ошибок и логирование

## 📋 Требования

- Node.js версии 14 или выше
- Minecraft сервер (локальный или удалённый)
- Установленные зависимости npm

## 🔧 Установка

1. Установите зависимости:
```bash
npm install
```

2. Настройте конфигурацию в файле `bot.js` или используйте переменные окружения:
```bash
export MC_HOST=localhost
export MC_PORT=25565
export MC_USERNAME=BotBibob
export MC_VERSION=1.19.4
```

## ▶️ Запуск

```bash
npm start
```

или

```bash
node bot.js
```

## 🎮 Команды в чате

Бот реагирует на команды, начинающиеся с `!`:

- `!inv` или `!inventory` - показать инвентарь
- `!pos` или `!position` - показать текущую позицию
- `!health` или `!hp` - показать здоровье и голод
- `!follow <имя>` - следовать за указанным игроком
- `!stop` или `!stay` - остановиться
- `!goto <x> <y> <z>` - идти к указанным координатам
- `!craft <предмет> [количество]` - скрафтить предмет
- `!get <предмет> [количество]` - получить предмет (добыть/скрафтить)
- `!help` - показать список команд

## ⚙️ Конфигурация

В начале файла `bot.js` можно изменить настройки:

```javascript
const CONFIG = {
  HOST: 'localhost',           // Адрес сервера
  PORT: 25565,                 // Порт сервера
  USERNAME: 'BotBibob',        // Имя бота
  VERSION: '1.19.4',          // Версия Minecraft
  AUTO_RECONNECT: true,        // Автопереподключение
  RECONNECT_DELAY: 5000,       // Задержка перед переподключением (мс)
  CHAT_COMMANDS: true          // Включить команды в чате
}
```

## 📝 План действий

По умолчанию бот выполняет следующий план:
1. Собрать 2 дубовых бревна
2. Собрать 3 булыжника
3. Скрафтить каменную кирку
4. Собрать ещё 8 булыжников
5. Скрафтить печь

План можно изменить в функции `executePlan()` в файле `bot.js`.

## 🛠️ Функции бота

### Добыча ресурсов
- Автоматический поиск и добыча блоков
- Выбор лучшего инструмента для добычи
- Обработка таймаутов и ошибок

### Крафт
- Рекурсивная добыча ингредиентов
- Автоматическое создание верстака при необходимости
- Поддержка всех рецептов Minecraft

### Передвижение
- Умный pathfinding с обходом препятствий
- Следование за игроками
- Перемещение к указанным координатам

## 📊 Логирование

Бот выводит подробные логи всех действий с временными метками:
- ✅ Успешные операции
- ❌ Ошибки
- ⚠️ Предупреждения
- 💬 Сообщения в чате
- 📍 Позиции и события

## 🔒 Безопасность

- Бот работает в офлайн режиме (auth: 'offline')
- Для онлайн серверов может потребоваться настройка авторизации
- Не хранит пароли или токены

## 🐛 Решение проблем

### Бот не подключается
- Проверьте, что сервер запущен и доступен
- Убедитесь, что порт правильный
- Проверьте версию Minecraft

### Бот не выполняет команды
- Убедитесь, что `CHAT_COMMANDS: true` в конфигурации
- Команды должны начинаться с `!`
- Проверьте логи на наличие ошибок

### Бот не добывает блоки
- Убедитесь, что бот в режиме выживания (survival)
- Проверьте, что у бота есть инструменты
- Убедитесь, что блоки находятся в радиусе поиска (64 блока)

## 📚 Дополнительная информация

- [Mineflayer документация](https://github.com/PrismarineJS/mineflayer)
- [Mineflayer Pathfinder](https://github.com/Karang/mineflayer-pathfinder)

## 📄 Лицензия

ISC

